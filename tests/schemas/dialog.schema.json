{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://sipnab.com/schemas/dialog.schema.json",
  "title": "sipnab dialog summary (GET /v1/dialogs list item)",
  "description": "Serialized form of output::model::DialogSummary — the canonical compact dialog projection shared by the REST API, MCP, and TUI-save surfaces (WS3). One element of the `dialogs` array; the list wrapper carries schema_version/total/offset/limit. WS3 wire change: `from`/`to` became `from_user`/`to_user`, and `timing` gained `duration_ms`.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "call_id",
    "state",
    "method",
    "duration_sec",
    "msg_count",
    "timing",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "call_id": { "type": "string" },
    "from_user": { "type": ["string", "null"] },
    "to_user": { "type": ["string", "null"] },
    "state": { "type": "string" },
    "final_status_code": { "type": "integer" },
    "final_status_reason": { "type": "string" },
    "method": { "type": "string" },
    "duration_sec": { "type": "number" },
    "msg_count": { "type": "integer", "minimum": 0 },
    "timing": {
      "type": "object",
      "additionalProperties": false,
      "required": ["retransmits"],
      "properties": {
        "pdd_ms": { "type": ["integer", "null"] },
        "setup_ms": { "type": ["integer", "null"] },
        "retransmits": { "type": "integer", "minimum": 0 },
        "duration_ms": { "type": ["integer", "null"] }
      }
    },
    "created_at": { "type": "string", "format": "date-time" },
    "updated_at": { "type": "string", "format": "date-time" },
    "frame": {
      "type": "string",
      "description": "Pointer to the frame the dialog opened in, as `<source>#<ordinal>@<digest>` — feed it to `sipnab --show-frame` to retrieve the bytes, which either returns the frame or refuses because the capture changed. Optional, and absent rather than null when the dialog has no frame (live capture). Not in `required` for that reason: an absent key means unknown, and a present one is always a real pointer.",
      "pattern": "^.+#[0-9]+(@[0-9a-f]{16})?$"
    },
    "input_origin": {
      "type": "string",
      "enum": [
        "wire",
        "hep",
        "uprobe"
      ],
      "description": "Which capture source delivered the message that OPENED this dialog. First, never latest: one process can capture from an interface and a HEP mirror at once, and with no BPF filter excluding the mirrored signaling ports the same message arrives from both, so a field reassigned per message would report whichever spoke last. Absent rather than null when the opening message came from no captured packet."
    }
  }
}
